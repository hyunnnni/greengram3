<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.greengram3.user.UserMapper">
    <insert id="userInsSignup" useGeneratedKeys="true" keyProperty="iuser">
        INSERT INTO t_user
        set uid = #{uid},
        upw = #{upw},
        nm = #{nm},
        pic = #{pic}
    </insert>

    <select id="userSelSignin">
        SELECT iuser, nm, pic
        from t_user
        where uid = #{uid}
    </select>

    <select id="userSelUpw">
        SELECT upw
        FROM t_user
        where uid = #{uid}
    </select>

    <delete id="delFollow">
        delete from t_user_follow
        where from_iuser = #{fromIuser} and to_iuser = #{toIuser}
    </delete>

    <insert id="insFollow">
        insert into t_user_follow
        set from_iuser = #{fromIuser},
        to_iuser = #{toIuser}
    </insert>
</mapper>